# 测试问题整理

以下为“工作中遇到的实际问题描述”的汇总表，便于评估、分配和跟踪处理进度。

| 序号 | 问题（工作中遇到的实际描述） | 原始要点/备注 |
|---:|---|---|
| 1 | 系统在界面上默认将业务状态标示为 Open，且不应默认展示 Cancel 状态给用户查看造成混淆。 | 默认为行业务关闭=Open，不需要显示Cancel |
| 2 | 表单字段标签不准确：填写批号文案需明确为供应商批号。 | 【填写批号】，文字改成【供应商批号】 |
| 3 | 表单字段文案不一致：填写制造日期文案应规范为制造日期。 | 【填写制造日期】文字改成【制造日期】 |
| 4 | 按钮文案不清晰：批量维护日期应更明确为批量维护制造日期。 | 按钮【批量维护日期】文字改成【批量维护制造日期】 |
| 5 | 系统允许直接打印托标签但未阻止，导致报错提示“不可直接生成并打印托标签”。 | 直接打印托标签，系统要报错，“不可直接生成并打印托标签” |
| 6 | 标签类型划分不明确，应区分托标签与箱标签；系统未提供物料拉动单位下的多产线区分。 | 标签请直接分托标签和箱标签，LAC不存在物料拉动单位的标签 |
| 7 | ASN 查看缺少必要字段展示，需要增加供应商批号与制造日期两列。 | ASN查看中，请增加供应商批号、制造日期2列数据显示 |
| 8 | 页面字段 Doc No. 文案不准确，应改为 ASN No。 | Doc No.改成ASN No |
| 9 | 英文版 WebPortal 中部分字段仍显示中文（如 ASN 状态、标签类型、行状态）。 | 英文版本的WebPortal中，【ASN的状态】、明细中【标签类型】（箱托）、【行状态】，目前显示的中文，请改成英文 |
| 10 | 830 下推生成 ASN 时，制造日期默认为空，不应自动填默认值，需由用户手动选择。 | 830下推ASN，制造日期默认为空，由用户手工选择填写，目前的默认值不要。 |
| 11 | PDA 绑定托标签后移动打印流程异常：PDA 扫箱标签后移动打印机应能打印托标签但目前无法稳定实现。 | PDA绑定托标签后，PDA可打印托标签（移动打印机） (述求：外网PDA+外网移动打印机，扫描箱标签，移动打印机可打出托标签） |
| 12 | ASN 模糊查询应支持按料号缩小范围并快速定位相关 ASN。 | 可以根据料号，缩小范围，找到相关的ASN。 |
| 13 | 页面应显示 LAC 自动生成的批号，并确保标签打印为 LAC 批号而非供应商批号。 | 页面上希望能看到LAC自动生成的批号，然后标签上打印的是LAC的批号，不是供应商批号，详见PIC |
| 14 | 系统中缺少 ASN 字段导致检索不便，应增加 ASN 字段以便快速查找。 | 希望增加ASN字段，方便快速查找 |
| 15 | WMS 的 ASN 显示应包含 ERP 的收料通知单号，便于关联查询。 | WMS的ASN，显示ERP的收料通知单号，方便关联查询 |
| 16 | 表头字段文案需统一：预到货通知单单号改为 ASN 号。 | 表头界面，字段名【预到货通知单单号】改成【ASN号】 |
| 17 | 按钮文案需统一：批量生成预到货通知单改为批量生成ASN。 | 按钮【批量生成预到货通知单】改成【批量生成ASN】 |
| 18 | 供应商查询结果包含太多类型，应仅列出公司类别为 Manufacturing 的供应商。 | 供应商查询，只需要列出公司类别是Manufacturing的供应商，不需要全部 |
| 19 | 删除权限后，PO 下推 830 导致 批量释放 按钮丢失，影响流程。 | 采购订单下推ASN,功能无用，但是权限删除后，PO下推830，【批量释放】按钮就没了。 |
| 20 | WMS 中存在数量丢失或字段取值错误（如数量 300 丢失，30 的到货日期丢失，且到货日期取错 ERP 字段）。 | 数量300的行,WMS中丢了，数量30的【到货日期】数据丢了。到货日期取错ERP字段。 |
| 21 | 单位列展示内码而非可识别单位（应显示 PC、G 等）。 | 单位列不能显示内码，应该显示具体的单位，如PC，G |
| 22 | 多界面缺少单据类型列（PPAP/PSMG），影响筛选与识别。 | 830需求、830下推ASN、ASN、外购入库，4个界面增加单据类型列（PPAP/PSMG),同采购订单下推830 |
| 23 | 非箱追溯供应商应允许在线打印箱/托标签；箱追溯在未绑定托标签前不能查看托标签。 | "非箱追溯的，供应商也可以在线打印箱标签和托标签; 箱追溯的，未绑定托标签时，无法查看托标签，绑定完以后，可以在线打印（重打）箱标签和托标签" |
| 24 | 审核后重新生成标签时，批号与制造日期有时未显示或丢失。 | 已经完成审核后，标签上未显示批号和制造日期，重新生成后，有数据 |
| 25 | 未维护制造日期时系统允许打印标签，导致标签缺失关键信息；应阻止打印或提示。 | 需求：未维护制造日期，系统无法打印标签 |
| 26 | 更改包装方案后系统未正确计算每托箱数，导致每托箱数变为 0（计算错误）。 | 原包装方案每箱12件，每托50箱，更改成每箱15件后，每托箱数自动更新成了0（1031193A01） |
| 27 | 供应商角色权限控制异常：设定为供应商专属仍可查看所有供应商数据。 | 我设定了LAC01是力博特的供应商专属，可是账号进去，一路畅通，显示所有供应商。（2个830界面，ASN界面是OK的） |
| 28 | 标签生成权限应限制供应商仅能看到并生成自家标签，目前未生效。 | 标签生成，供应商角色，只能看到供应商标签，且他们家自己的。 |
| 29 | Purchase Order Num 拼接逻辑不正确，需包含 EDI 固定合同号 + 属性( PPAP/PSMG )。 | Purchase Order Num，【EDI固定合同号】+PPAP或PSMG属性拼接，详见当前系统截图参考，PIC |
| 30 | 发送邮件字段拼接与取值错误（Contact Name、Forecast Net Qty、UOM、Forecast Date、Release Date 等取错字段）。 | 发送的邮件，Purchase Order Num拼接不对（固定号-WW-PPAP）；Contact Name联系人取计划员；Forecast Net Qty数量（PO/PR),；UOM取实际单位，非内码；Forecast Date取DeliveryDate（PO或PR)；Release Date释放830的日期 |
| 31 | 展望期（Forecast）不显示，影响预测与计划。 | 展望期未显示 |
| 32 | ERP 中已关闭的 PO/PR 仍显示在系统中，应屏蔽或不显示。 | ERP中已关闭的PO和PR不要显示 |
| 33 | 系统误报无包装方案（但实际存在），导致下推失败或异常。 | 系统误报没有包装方案，经查有包装方案，1000659C02（直接下推正常） |
| 34 | 查询项需要增加供应商与联系人筛选字段以便精确定位。 | 查询项,增加供应商、联系人的查询 |
| 35 | 页面需仅显示 F（Firm PO）行，P（Plan PR）行不应显示。 | 只需要显示F(Firm PO)行，不需要P（Plan PR)行 |
| 36 | 保存成功提示框不会自动关闭，需用户手动点关闭，影响体验。 | 弹框告知，已保存成功，需要用户点关闭 |
| 37 | ASN 取消后 ERP 收料通知单状态已变更，但 830 不需要重新发送，取消数量回退处理不明确。 | ASN取消后，ERP的收料通知单状态显示正确（更新为“已作废”，830不需要重新发送，取消数量需要重新退回830，可以立即重做ASN，不需要MC重新释放新830 |
| 38 | 同料号但不同采购订单类型（PPAP/PSMG 与普通订单）同日期不应合并为一行。 | 同料号、不同采购订单类型（PPAP/PSMG和普通订单），同日期，不能合并，要分2行 |
| 39 | 无包装方案料号默认以 9999 计算托/箱数量，但供应商录入后应重新按其包装方案计算；默认 9999 不允许审核。 | 无包装方案的料号，先按9999计算原始托和箱的张树...默认9999的包装方案，不允许审核 |
| 40 | 系统强制每托/每箱最小数量限制（≥2）不明确，输入 1 时应报错且提示原因。 | 系统限制：每托数量必须≥2、每箱数量必须≥2，避免大量标签影响性能,如果供应商输入1，报错,不是默认=2 |
| 41 | 供应商选择筛选应只显示制造型供应商（BusinessFunction='Manufacturing'）。 | 选择供应商，只需要显示制造型的供应商(BusinessFunction='Manufacturing') |
| 42 | 不同供应商可使用相同 ASN 编号导致识别冲突，需要确认是否允许。 | 不同供应商，可以使用相同的ASN号码 |
| 43 | Excel 导入日志误报“导入成功”但实际未导入（PSMG、供应商-料号映射出错），日志与导入结果不一致。 | (1)Excel中填写PSMG，其实无PSMG订单，未导入成功,日志说导入成功，其实并没有导入；(2)供应商和料号对应错了，日志说导入成功，其实并没有导入，Process有文件 |
| 44 | 830 取值应使用 Delivery Date（交付日期）而非到货日期字段，字段名需避免歧义。 | 830取Delivery Date，不是到货日期。来自云星空的Delivery Date，字段名不要显示交货日期，有歧义 |
| 45 | 下推数量计算与净需求字段重复或默认值错误，导致可下推数量显示不正确。 | 详见PIC，净需求500，可下推数量和默认值错误，可下推数量列和净需求重复，删除可下推列 |
| 46 | 各类导入操作在失败时仍被标记为成功，日志缺乏详细失败原因（如 MFGDate、PO 不足、料号不存在等）。 | 各种导入并归档失败，目前日志都报成功：... |
| 47 | ERP 同 PO 不同行但相同日期未合并处理，导致数据重复。 | ERP同PO不同行，同日期，未合并，详见PIC |
| 48 | 手工选择多行生成托标签时操作逻辑异常，可能导致无法生成或分成多单。 | 手工选择2行，导致无法生成托标签；第二次补扫，分成了2单 |
| 49 | 采购订单下推 830 应只下推 F 状态行，不应包含 P/计划行。 | 采购订单下推830，含P&F；830下推ASN，只需要F,不需要P |
| 50 | 扫描/绑定箱标签时未按包装方案自动生成托标签，满箱应自动生成托标签，非满托由人工触发。 | 绑定箱标签的时候，根据包装方案，满箱数，自动生成托标签，无需人工生成;不满托，人工触发生成 |
| 51 | 已扫描复选框无法手工取消，造成操作灵活性受限。 | 已扫描的复选框，手工无法取消 |
| 52 | 明细字段格式与单位展示问题：制造日期含时间、小数数量显示、小料号显示内码等均需优化。 | 详见PIC.红色错误；绿色需要优化 制造日期，仅日期，不需要时间；数量整数，不需要小数位；备注是PPAP/PSMG，需要此信息。材料显示具体的材料，非内码；采购类别，显示中文的外购或委外；运输方式未获取到 |
| 53 | 生成 830 时若打开了先前生成的 CSV 文件会报错，需提示并阻止或处理。 | 当生成新的830时提示报错打开了之前生成的830 csv 文件， 见截图 |
| 54 | 生成 830 时应取未交货数量而非订单总量，避免数量计算错误。 | 应该取未交货数量，当前取的订单数量 |
| 55 | 两版 830 生成存在时间差导致数据不一致，生成应实时取系统数据以消除时间差。 | 两版830生成时取值存在5分钟时间差，需求取消时间差，生成830文件时实时拿系统数据。测试文件参考PIC |
| 56 | 从 FTP 获取 856 文件后，无论成功或失败都被剪切至本地且不保留在 FTP，导致追踪困难。 | 从FTP获取856文件后，不管成功还是失败，都剪切掉，存到本地盘，不保留在FTP上 |
| 57 | ASN 导入成功应先扣减采购订单数量，当前流程依赖导入次序导致扣减不一致。 | 只有ASN导入成功后才能扣减采购订单数量,已经提供案例 |
| 58 | 整单 ASN 导入要求严格：若任一料号不符合要求则整单不能进入 WMS，导致某些符合项被阻塞。 | 整单ASN导入成功才能进入WMS系统，单个料号符合要求其他有料号不符合要求，不能导入ASN及记录序列号信息，已经提供案例 |
| 59 | 供应商 ASN 显示的编号错误，需通过 shipment ID 进行抓取定位问题。 | 供应商的ASN号显示不对, 抓shipment ID见截图 |
| 60 | 报错信息不够具体，无法快速定位问题根因。 | 报错提示信息不足 |
| 61 | 856 成功/失败文件未加明显前后缀或标识，无法快速识别处理状态。 | 856 成功与失败的文件都需要添加前缀或后缀，加以识别 |
| 62 | LAP3 的 856 文件无法导入且无任何错误提示或邮件告警，需排查并补偿告警机制。 | LAP3 的856无法导入，没有任何提示。解决方案？LAP1 和 LAP3 的DUNS Code 是一样的 |
| 63 | ASN 的确认状态应使用“是/否”而非模糊状态，便于自动化处理。 | ASN的是否确认，改成是和否。 |
| 64 | PDA 提交后未能自动生成已审核的入库单，导致后续入库流程缺环节。 | PDA提交后，需要生成已审核的入库单 |
| 65 | ASN 界面应支持对 LAC 内部用户单独授权以补打标签。 | ASN界面增加单独授权给LAC内部用户，可以补打印标签 |
| 66 | 供应商来料标签上制造日期包含时分秒，应仅保留日期部分。 | 供应商来料标签，二维码制造日期，只需要到日期，不需要到时分秒（12:00AM) |
| 67 | ASN 回写 ERP 时未包含 ASN 字段，需增加回写字段以保持追溯性。 | ASN回写ERP的时候，增加ASN字段的回写 |
| 68 | LAP3 的 856 导入无报错邮件且 FTP 文件被剪切，导致无法在系统或邮件中追踪异常。 | LAP3 的856的导入，没有报错邮件，FTP已被剪切，隔离和成功的文件夹下面都没有。WMS ASN里看不到，云星空里的订单已经被吃掉了 |
| 69 | ASN 明细列界面需要调整与确认详细展示列（等待提交详细界面图）。 | ASN明细里调整列界面，等冬芹提交详细界面图 |
| 70 | 明细界面应增加 入库 按钮（整单入库流程），以便快速操作。 | 明细界面里，增加【入库】按键（还是整单入库，非行入库） |
| 71 | 电脑端明细中不应显示 仓库位置内码ID 字段，应予以移除。 | 电脑端明细里的【仓库位置内码ID】移除 |
| 72 | 标签条目列表应显示料号列便于核对，而非仅内码或隐藏信息。 | 标签条目界面中，增加料号列的显示 |
| 73 | 外购入库界面查询需联合表头与表体信息并支持导出功能。 | 外购入库界面，需要联合表头和表体信息，包括查询也是联合查询，增加导出按钮 |
| 74 | 生成装箱单的预览打印界面需显示 ASN 号以便对照与核验。 | 生成装箱单后，预览打印界面，增加ASN号码 |
| 75 | 行复选框未默认选中，易导致用户手动遗漏操作，应避免误操作。 | 选中行，复选框不点上，避免用户手工误操作 |
| 76 | 委外订单状态显示错误（应为 F，但显示为 P），导致无法下推 ASN。 | 委外订单，应该是F的，结果显示成P，无法做ASN |
| 77 | 箱追溯供应商扫描 ASN 入库时未提示必须扫描托条码，导致漏扫问题。 | 箱追溯供应商，扫描ASN号入库，系统无提示必须扫描托 |
| 78 | ASN 界面应支持区分外购与委外属性以便流程区分。 | 增加ASN的外购和委外属性 |
| 79 | ASN 界面布局需优化（首列冻结复选框、物料号检索、界面调整）。 | "1.界面调整，如PIC. 2.ASN预到货通知单界面能够显示物料号或者有检索物料号的功能。3.复选框需要作为首列冻结住。" |
| 80 | 830 界面复选框无用，应移除以简化界面。 | 830复选框没有用，请移除 |
| 81 | 聚合关系查询应支持按托条码查询，目前仅支持箱条码。 | Label Code Aggregation聚合关系，增加托条码查询（目前只能搜到箱条码，找不到托条码） |
| 82 | 委外 PR 在 830 下推界面不可见，导致 F 状态的 PR 无法下推。 | 委外PR下达状态，F状态，但是不能下推ASN,详见PIC(830可见，下推界面不可见），830查看是需区分行。F的PR，830查看可见；830下推不可见 |
| 83 | ASN 生成未确认导致系统报错提示 PO 数量异常，需检查回写与确认流程。 | ASN生成未确认，系统报错无PO数量，经查, ERP有收料通知单（80W); 详见PIC |
| 84 | 830 列表针对已确认 ASN 不应再显示为 0 的值，影响判断。 | 830针对已经确认的ASN，不需要再显示为0的830 |
| 85 | 英文版部分字段（Object Type, Business Partner）缺少英文翻译。 | 英文版中，Object Type, Business Partner，无英文 |
| 86 | 箱追溯供应商若漏扫某托应告知具体是第几托漏扫，便于补扫。 | 箱追溯供应商，如果漏扫了1托，需要告知第几托漏扫。 |
| 87 | 生产与包材 ASN 应分开显示，标准 ASN 不应展示包材信息。 | 生产和包材ASN已分开，标准ASN中不希望看到包材的ASN信息 |
| 88 | 多项筛选功能失效（如 Planning Schedule Request、Purchase Return 等），影响查询。 | Planning Schedule Request|830需求、Pend Check|待检清单、Purchase Return|采购退料、Transfer To Settlement|寄售运输下推寄售结算、Transfer to Transfer | 寄售调拨下推寄售退回、Consign Return | 寄售退回、Kanban Definition | 看板定义、Goods Issue Query | 发货通知单、的筛选功能失效，详见PIC |
| 89 | ASN 标签的交货日期应取 预计发车时间 的日期，且包装类型数据结果不正确。 | ASN标签,交货日期，取【预计发车时间】的日期，包装类型数据结果不对 |
| 90 | ASN 的单据日期使用不当，当前显示为创建时间而非 830 的单据日期。 | ASN的单据日期，是创建的日期，不是830的日期 |
| 91 | 取消并重新生成 ASN 后追加行导致总托数为 0，重新生成标签仍显示 0。 | ASN取消后，在新的ASN上做追加行，总托数全部显示为0，重新生成标签也依旧为0，（物料代码：1004637C01）详情请见PIC686 |
| 92 | 对已取消的采购退货单操作异常，取消未审核的单据时提示异常无法正常取消。 | 17.目前：状态为open的，取消后，提示异常，希望可以正常取消未审核的采购退货单。详见PIC |
| 93 | 过滤条件默认 是否取消=False，未明确说明，导致筛选误解。 | 18.过滤条件，默认是否取消=False，默认不看取消。 |
| 94 | 已取消的 ASN 不应允许入库操作，应禁用入库入口。 | 19.已经取消的ASN，不可以做入库； |
| 95 | 追加行时可以输入供应商批号但保存后为空，回写失败。 | 追加行,可以输入供应商批号，但是保存后为空 |

---
